eec	() { cat $1 | python ../src/parser.py | tee ${1%.eec}.out 2> ${1%.eec}.err; }
dot_init () 
{ 
    git status;
    PREPY_PATH=${PREPY_PATH:=$PATH};
    PATH=$PWD/bin:$PREPY_PATH;
    export PREPY_PATH;
    doit ls src/testdata*.eec;
    functions ./.dotrc | grep -v _init
}
dot_init 1>&2
